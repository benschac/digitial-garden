import { Typography, typographyVariants } from "@/components/page-typography";
import { PageTransition } from "@/app/_components/page-transition";
import { Eyebrow } from "@personal-site/ui/components/eyebrow";
import { PageNavigation } from "@personal-site/ui/components/page-navigation";
import Link from "next/link";
import { ForcesExperiment } from "./forces-experiment";
import styles from "./forces-styles";

export const metadata = {
  title: "Newton's Laws, in Motion",
  description: "An interactive canvas laboratory for Newton's three laws of motion.",
  alternates: { canonical: "/experiments/forces" },
};

<PageTransition>
<main className={typographyVariants({ variant: "experimentPage", className: styles.page })}>
  <PageNavigation className={styles.nav}>
    <Link className={styles.navLink} href="/" transitionTypes={["nav-back"]}>← Workbench</Link>
    <span>Experiment 004</span>
  </PageNavigation>

  <header className={styles.header}>
    <Eyebrow className={`mb-4 ${styles.eyebrow}`}>Classical mechanics · Canvas · MDX + LaTeX</Eyebrow>
    <Typography as="h1" className={styles.title} variant="experimentDisplay">Every motion has an account.</Typography>
    <Typography as="p" variant="experimentForcesIntro" className={styles.intro}>
      {"Change the conditions and watch the bookkeeping update. Velocity persists, force changes momentum, and every push returns an equal push."}
    </Typography>
  </header>

  <ForcesExperiment />

  <section className={styles.laws} aria-labelledby="field-notes">
    <Eyebrow className={`mb-4 ${styles.eyebrow}`}>Field notes</Eyebrow>
    <Typography as="h2" variant="experimentForcesSectionHeading" className={styles.lawsTitle} id="field-notes">Three laws, one system.</Typography>

    <div className={styles.lawGrid}>
      <article className={styles.law}>
        <span className={styles.lawNumber}>01 / Inertia</span>
        <h3 className={styles.lawTitle}>No net force, no change in motion.</h3>
        <p className={styles.lawDescription}>
          {"An object at rest stays at rest, and a moving object keeps a constant velocity, unless a net external force acts on it."}
        </p>

        <div className={typographyVariants({ variant: "experimentForcesEquation", className: styles.equation })}>
          $$
          \sum \vec{F} = 0 \quad \Longrightarrow \quad \frac{d\vec{v}}{dt} = 0
          $$
        </div>
      </article>

      <article className={styles.law}>
        <span className={styles.lawNumber}>02 / Acceleration</span>
        <h3 className={styles.lawTitle}>Force changes momentum.</h3>
        <p className={styles.lawDescription}>
          {"For constant mass, acceleration points with the net force. Double the force and acceleration doubles; double the mass and it halves."}
        </p>

        <div className={typographyVariants({ variant: "experimentForcesEquation", className: styles.equation })}>
          $$
          \sum \vec{F} = \frac{d\vec{p}}{dt} = m\vec{a}
          $$
        </div>
      </article>

      <article className={styles.law}>
        <span className={styles.lawNumber}>03 / Interaction</span>
        <h3 className={styles.lawTitle}>Forces arrive in pairs.</h3>
        <p className={styles.lawDescription}>
          {"When body A pushes body B, body B pushes body A with the same magnitude in the opposite direction. The forces act on different bodies."}
        </p>

        <div className={typographyVariants({ variant: "experimentForcesEquation", className: styles.equation })}>
          $$
          \vec{F}_{A \to B} = -\vec{F}_{B \to A}
          $$
        </div>
      </article>
    </div>
  </section>

  <aside className={typographyVariants({ variant: "experimentForcesNotes", className: styles.notes })}>
    <p className={styles.note}>
      {"This is an idealized, frictionless model. The values are internally consistent, but the canvas maps simulation units to pixels for legibility."}
    </p>
  </aside>
</main>
</PageTransition>
